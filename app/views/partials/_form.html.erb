<%= form_for [@city, @post] do |f|%>
  <%= f.label :title %>
  <%= f.text_field :title %>
  <%= f.label :content %>
  <%= f.text_field :content, :cols => "10", :rows => '10'%>
  <%= f.submit %>
<% end %>